{
  "name": "tk-cli",
  "version": "0.1.0",
  "description": "Personal ticket management CLI",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "tk": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/tk",
    "test": "bun test",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
